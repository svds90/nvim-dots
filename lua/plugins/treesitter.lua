return {
  ensure_installed = { "lua", "query", "python", "luadoc", "printf", "vim", "vimdoc" },

  playground = {
    enabled = true,
  },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}
